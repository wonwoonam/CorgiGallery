//
//  NavigationController.swift
//  GalleryX
//
//  Created by Won Woo Nam on 9/18/21.
//

import Foundation
import UIKit
class NavigationController: UINavigationController{
   

    override func viewDidLoad() {
 
        /*
        self.navigationBar.backIndicatorImage = UIImage(named: "backButtonIcon")
        self.navigationBar.backIndicatorTransitionMaskImage = UIImage(named: "backButtonIcon")
        self.navigationBar.tintColor = .black
    
        if (defaults.string(forKey: "user_id") != nil) {
            print(defaults.string(forKey: "user_id"))
            let storyboard = UIStoryboard(name: "Main", bundle: Bundle.main)
            let mainTabC = storyboard.instantiateViewController(withIdentifier: "MainTabBarC") as! StartingViewController
            self.isNavigationBarHidden = false
            self.navigationBar.isTranslucent = false
            self.pushViewController(mainTabC, animated: true)
        }else{
            let storyboard = UIStoryboard(name: "Main", bundle: Bundle.main)
            let mainTabC = storyboard.instantiateViewController(withIdentifier: "SignUpView") as! SignUpViewController
            self.isNavigationBarHidden = false
            self.navigationBar.isTranslucent = false
            self.pushViewController(mainTabC, animated: true)
        }
 */
        
    }
}
